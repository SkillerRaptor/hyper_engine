fn main() {
    let a = 0;
    let b = a + 10;
    let c = a + b;
    let d = a + b + c;

    let x = 10;
    let y = 20;
    print(x);
    print(y);

    let z = 0;
    if (x > y) {
      z = x - y;
    } else {
      z = x + y;
    }

    print(z);
}