fn main() {
    let i = 0;
    let total = 0;

    while (i < 10) {
        if (i == 5) {
            total = total + 100;
        } else {
            total = total + i;
        }
        i = i + 1;
    }

    print(total);
}